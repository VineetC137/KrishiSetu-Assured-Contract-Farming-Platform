import { Language, TranslationKeys } from '../i18n';
import { en } from './en';
import { hi } from './hi';
import { mr } from './mr';

export const translations: Record<Language, TranslationKeys> = {
  en,
  hi,
  mr,
};

export { en, hi, mr };